{
  "environment": {
    "TAVILY_API_KEY": "${TAVILY_API_KEY}"
  },
  "tests": [
    {
      "name": "Search web for AI news",
      "tool": "tavily_search",
      "arguments": {
        "query": "artificial intelligence"
      },
      "expect": {
        "type": "any"
      }
    }
  ]
}
