# E2E Test Environment Variables
# Copy this file to .env.e2e and fill in your values

# QA Bearer Token (required for test-qa.ts)
# Used for authenticated API requests to QA/staging environments
# QA_BEARER_TOKEN=your_bearer_token_here

# Finnhub API Key (optional - test will be skipped if not provided)
# FINNHUB_API_KEY=your_api_key_here

# PostgreSQL Database URL (optional - test will be skipped if not provided)
# DATABASE_URL=postgresql://user:password@localhost:5432/dbname